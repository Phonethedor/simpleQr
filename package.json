{
    "name": "ts-qr-code",
    "private": true,
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview",
        "predeploy": "npm run build",
        "deploy": "gh-pages -d dist"
    },
    "dependencies": {
        "qrcode": "^1.5.3"
    },
    "devDependencies": {
        "@types/qrcode": "^1.5.5",
        "gh-pages": "^6.3.0",
        "typescript": "^5.4.0",
        "vite": "^5.2.0"
    }
}